# Supabase (the records store) — service key is server-side only
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Claude analysis layer (the verify gate) — from console.anthropic.com
ANTHROPIC_API_KEY=

# Helius RPC — pulls a wallet's history for verification
HELIUS_API_KEY=
